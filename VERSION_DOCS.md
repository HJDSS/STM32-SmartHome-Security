# 智能安防与智能家居系统 — 版本说明文档

## 版本信息

| 项目 | 内容 |
|------|------|
| **版本号** | M1.15 |
| **分支** | `rescue/auto-rollback-20260427-0000` |
| **最新提交** | `a3434257` — fix(M1.15d): 第3批 |
| **提交日期** | 2026-05-18 |
| **主芯片** | STM32F103RCT6 (Cortex-M3, 72MHz, 256KB Flash, 48KB RAM) |
| **RTOS** | FreeRTOS V9（可配置为裸机模式） |
| **开发环境** | Keil MDK (ARMCC V5) |

---

## 1. 系统概述

本系统是一套**集成式智能安防与智能家居控制器**，基于单颗 STM32F103RCT6 芯片，运行 FreeRTOS 实时操作系统，通过 ESP8266 模块接入 OneNET 云平台，实现本地与远程协同的安防监控、环境感知与设备联动。

### 1.1 主要功能一览

- **门禁管理**：密码键盘（4×4 矩阵）+ 指纹识别（AS608），用户/管理员双权限，管理员功能键（录入/清库/改密）
- **键盘功能键**：A/B/C/D 双模式——管理员态（录入/清库/退出/改密）& 普通态（AWAY/HOME/日志/紧急）
- **安防模式**：DISARM / HOME（本地蜂鸣） / AWAY（联动+抓拍+云告警）三种模式
- **暴力破解告警**：5 次错密→强制 AWAY+蜂鸣 3s+OneNET 上行
- **环境监测**：温度、湿度（DHT11）、可燃气体（MQ-2）、人体红外（HC-SR501）
- **安防布撤防**：键盘一键布撤防 + OneNET 远程布撤防
- **摄像头抓拍**：OV7670 + AL422B FIFO，VGA 分辨率，BMP 格式存入 SD 卡
- **智能联动**：开门自动亮灯、入侵全灯开启+语音告警、燃气泄漏自动关阀
- **物联网连接**：ESP8266 AT 固件 → MQTT → OneNET 云平台，双向数据/命令通道
- **OLED 显示**：0.96 寸 SSD1306/SH1106，仪表盘式界面，网络诊断面板，日志查看
- **系统日志**：Flash 循环存储 256 条事件，USART1 带标签日志输出，JSON 统计导出
- **安全机制**：密码锁定（3 次错锁 30s）、暴力破解告警（5 次）、MQTT 命令去重、AES-128 加密、IWDG

---

## 2. 硬件拓扑

| 外设 | 型号 | MCU 引脚 |
|------|------|----------|
| 指纹模块 | AS608 (UART, 57600bps) | PA9 (TX), PA10 (RX), PA8 (状态) |
| WiFi 模块 | ESP8266 / ESP-01S (115200bps) | PB10 (TX), PB11 (RX), PC9 (EN) |
| 摄像头 | OV7670 + AL422B FIFO | PA0–PA7 (数据), PA11 (VSYNC), PA12 (RCK), PB3–PB6 (FIFO 控制), PD2 (SCL), PB7 (SDA) |
| OLED 屏 | 0.96" SSD1306/SH1106 (软件 I2C) | PB8 (SCL), PB9 (SDA) |
| SD 卡 | MicroSD, SPI 模式 (FatFs) | PB12 (CS), PB13 (SCK), PB14 (MISO), PB15 (MOSI) |
| 温湿度 | DHT11 (单总线) | PC5 |
| PIR 红外人感 | HC-SR501 (EXTI) | PC4 |
| 燃气 | MQ-2 (ADC1_IN8) | PB0 |
| 键盘 | 4×4 矩阵键盘 | 行: PC0–PC3, 列: PC11, PC12, PB1, PB2 |
| 门锁继电器 | 继电器模块（低电平触发） | PC6 |
| 燃气阀门继电器 | 继电器模块 | PC7 |
| 照明继电器 | 继电器模块 | PC8 |
| 蜂鸣器 | 有源蜂鸣器 | PC10 |
| 调试串口 | USB-TTL (与 AS608 共用 USART1) | PA9 (TX), PA10 (RX) |

---

## 3. 软件架构

### 3.1 FreeRTOS 任务划分

| 任务名 | 优先级 | 周期 | 职责 |
|--------|--------|------|------|
| `Task_Lock` | 5 | 20ms | 键盘扫描、密码校验、指纹识别、门锁控制 |
| `Task_Sensor` | 3 | 10ms | DHT11 采集、MQ-2 采样滤波、PIR 状态机 |
| `Task_Net` | 4 | 50ms | ESP8266 AT FSM、MQTT 连接/订阅/发布、OneNET 属性上报 |
| `Task_OLED` | 2 | 100ms | 仪表盘刷新、密码输入界面、诊断页绘制 |
| `Task_Log` | 1 | 1s | Flash 日志写入、统计 JSON 导出 |

裸机模式下，主循环以 20ms 为周期顺序执行各模块。

### 3.2 通信协议栈

| 协议层 | 实现 | 用途 |
|--------|------|------|
| USART1 | 57600 8N1 | AS608 指纹协议 + 调试日志输出 + 命令行 |
| USART3 | 115200 8N1 | ESP8266 AT 指令集 + MQTT 数据通道 |
| 软件 I2C | 位敲打 | OLED (0x3C/0x3D) |
| SCCB | 位敲打 | OV7670 寄存器配置 |
| SPI2 | 硬件 SPI | SD 卡 FatFs 文件系统 |
| 单总线 | 专有时序 | DHT11 温湿度读取 |
| ADC1 | 通道 8 | MQ-2 模拟电压采集 |
| EXTI | 线 4 | PIR 人体检测中断 |
| MQTT 3.1.1 | ESP8266 AT 承载 | OneNET 云平台通信 |

---

## 4. 功能模块详述

### 4.1 门禁控制 (`lock_manager`)

- 6 位密码输入，支持**用户密码**与**管理员密码**，存储于 MCU 内部 Flash
- 连续 3 次输错后键盘锁定 30 秒
- 管理员会话持续 120 秒后自动退出
- 指纹匹配通过 USART1 与 AS608 协议交互，可增删指纹
- 开门保持 3 秒后自动落锁
- 支持**远程开锁**（OneNET 物模型属性下发）

### 4.2 传感器采集

**DHT11（温湿度）**
- 2 秒采样周期，带退避重试和故障自动恢复
- 支持最小二乘校准系数（Q15 定点）

**MQ-2（可燃气体）**
- 中值滤波 + 滑动平均双重滤波
- 温漂补偿（Q8 定点系数）
- 斜率变化率双判据报警
- 粘滞检测与自诊断

**HC-SR501（人体红外）**
- EXTI 边沿触发，带毛刺抑制
- 最小活跃时间 + 闭锁期防止误触发
- 安防撤防状态下触发记为**误报**（用于论文指标统计）

### 4.3 多传感器融合

加权证据打分制：PIR=60 分，MQ-2=70 分，摄像头运动=40 分，阈值=80 分触发入侵判定。

### 4.4 摄像头抓拍 (`Capture` / `capture_task`)

- OV7670 VGA RGB565 图像采集，通过 AL422B FIFO 缓冲
- 抓拍触发条件：入侵检测、燃气报警、键盘锁定
- 叠加 ARGB4444 文字水印后保存为 BMP 文件到 SD 卡
- 文件名格式：`CAP_<Tick>.BMP`
- 离线队列：最多 8 个待上传事件，网络恢复后回放

### 4.5 智能联动 (`linkage`)

| 触发条件 | 联动动作 |
|----------|----------|
| 开门 | 自动开灯 + MQTT 事件上报 |
| 入侵 | 全灯开启 + UART 语音告警 + MQTT 报警 |
| 燃气超标 | 自动关阀 (PC7) + OLED 显示 "VALVE CLOSED" + MQTT 报警 |
| 温湿度超限 | MQTT 告警上报 |
| 远程命令 | LIGHT=ON/OFF, ARM, DISARM 响应 |

### 4.6 OneNET 云平台连接 (`esp8266_onenet_mqtt`)

- 设备注册：产品 ID `1TVZ0qTJOE`，设备名 `STM32zhinengjiaju`
- 服务器：`mqtts.heclouds.com:8883`（SSL）
- 每 5 秒上报物模型属性：温度、湿度、燃气值、门锁状态、布防状态、报警状态、LED 状态
- 接收远程下发：开锁、布防、撤防、灯控
- 自动重连机制，MQTT FSM 子阶段状态暴露到 OLED（M1.11 新增）

### 4.7 OLED 显示 (`oled_view`)

- 仪表盘主界面：传感器数据、门锁、网络状态、警报
- 密码输入界面：星号掩码
- **网络诊断页面**（M1.7+）：ESP8266 FSM 状态实时显示
- **心跳计数器**（M1.8+）：OLED 第 3 行 F/T 心跳数
- **阻塞探针**（M1.9+）：OLED 第 3 行末字母显示主循环环节
- **MQTT 失败代码**（M1.11+）：OLED 直接显示 MQTT 连接失败子阶段

### 4.8 系统监控与日志

- **独立看门狗 (IWDG)**：10 秒超时，6 路标记源（传感器/警报/网络/UI/监控/保留）全部就绪后才喂狗
- **Flash 循环日志**：256 条 × 32 字节，起始地址 0x0801E000，记录开门/警报/配置变更
- **标签日志**：USART1 输出，分类标记 `NET` / `SD` / `SENSOR` / `ACT` / `STAT`
- **JSON 统计导出**：每 60 秒一行结构化 JSON，包含误报数、重连数、回放数、SD 写入失败数、异常计数

### 4.9 安全特性

- 密码锁定机制（3 次错 → 30 秒锁定）
- MQTT 命令去重（环形缓冲区 16 条，60 秒 TTL）
- 可选 AES-128 加密存储 (`APP_SECURE_AES_ENABLE`)
- 看门狗监督所有任务
- 上电时执行器进入安全状态

---

## 5. 场景模式

| 模式 | 常量 | 行为 |
|------|------|------|
| 居家 | `SCENE_MODE_HOME` | 安防部分激活，PIR 低灵敏度 |
| 离家 | `SCENE_MODE_AWAY` | 全安防模式，PIR 高灵敏度 |
| 夜间 | `SCENE_MODE_NIGHT` | 全安防 + 静默报警（不触发蜂鸣器） |

---

## 6. 编译配置

- **IDE**：Keil MDK (`程序.uvprojx`)
- **编译器**：ARMCC (ARM Compiler 5/6)
- **库**：STM32 标准外设库（非 HAL）、FreeRTOS V9、FatFs R0.11
- **源码路径**：`biye/代码/`（完整工程）、`biye/final_code/`（精选重构文件）
- **关键宏开关**：`board_config.h` 控制外设启用/禁用，`FreeRTOSConfig.h` 控制 RTOS/裸机切换

---

## 7. 版本演进摘要

| 版本 | 变更内容 |
|------|----------|
| M1.0 | 基础功能集：门禁、传感器、OLED、ESP8266、抓拍 |
| M1.5 | `app_init` 结尾初始仪表盘绘制 |
| M1.6 | 补充 ESP8266 EN(PC9) GPIO 初始化 |
| M1.7 | OLED 联网诊断页 |
| M1.8 | OLED F/T 心跳计数器 |
| M1.9 | main loop 单环节阻塞探针 |
| M1.10 | 修复 DHT11 关中断死循环 + SH1106 适配 |
| M1.11 | MQTT 子阶段失败代码暴露到 OLED |
| M1.12 | ESP8266/MQTT 重构 + app_rtos/watchdog 模块化 + OLED 诊断增强 |
| M1.12a | 修复 OV7670 测试图案 BUG + syslog Flash 页擦除 BUG |
| **M1.14a** | **键盘功能键 + PIR HOME/AWAY 分离 + 暴力破解告警 + 管理员态重构** |
| **M1.15a** | **论文-代码差距修复 #11: ESP8266 重连指数退避（上限60秒）** |
| **M1.15b** | **论文-代码差距修复: 指纹解锁接入 + IPC机制(Queue/EventGroup/Semaphore/Mutex) + 蜂鸣器分级告警(5模式) + AES-128 CBC/HMAC/双密钥窗口** |
| **M1.15c** | **论文-代码差距修复: Task_Log职责重配(P=3,S=1024,T=200ms) + FreeRTOS抓拍 + MQ-2中值+滑动平均滤波 + 自适应阈值基线(IIR+3σ)** |
| **M1.15d** | **论文-代码差距修复: MQ-2温漂补偿(Q8) + 5级分段浓度标定 + 多传感器融合评分(S=0.5ePIR+0.3eCAM+0.2eACC) + 性能指标计数器(11项) + board_config常量清理** |

---

## 8. 已知限制

### 8.1 保留的已知限制（M1.14a 及之前）
- DHT11 更新率限制为 1Hz（硬件限制），实际采样周期 2s
- ESP8266 AT 固件版本差异可能导致部分 AT 指令不兼容
- OV7670 抓拍时禁用全局中断约 200ms（FIFO 读取），会影响网络通信实时性
- SD 卡兼容性依赖卡品牌和 SPI 模式时序裕量
- OLED SH1106 与 SSD1306 的列偏移差异需在 `oled.h` 中手动切换
- 图片上传云端（方案2）已拆分但未实现，当前仅本地 SD 存储

### 8.2 M1.15 新增/修复后的剩余差距
- 移动端 uni-app 代码不在本仓库（可后续作为独立项目）
- SD 容量统计已修复（由 capture 统计代替）
- 蜂鸣器 PC10 无硬件 PWM 通道，使用软件定时器模拟
- 远程阈值配置和手动抓拍命令未实现（🟡 重要，待后续版本）

---

*文档更新日期：2026-05-18*
