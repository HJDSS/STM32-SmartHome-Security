# 广东工业大学工科毕设图表公式格式模板

以下模板可直接用于论文 `论文.tex` 或 Word 排版时统一图表公式格式。内容只涉及格式，不改变研究内容。

---

## 一、公式模板

### 1. 公式编号规则
- 按章编号，格式为 `(章号.序号)`，例如 `(3.1)`、`(4.2)`。
- 编号置于公式右侧行末。
- 正文引用统一写作：`见式(3.1)`。

### 2. 公式排版模板
```latex
\begin{equation}
A = B + C
\label{eq:sample}
\end{equation}
```

### 3. 长公式换行模板
- 优先在等号处换行。
- 其次在 `+`、`-`、`×`、`÷` 处换行。
- 换行后运算符号不得置于行首。

```latex
\begin{equation}
\begin{aligned}
A &= B + C + D \\
  &\quad + E + F
\end{aligned}
\label{eq:longsample}
\end{equation}
```

### 4. 公式字体要求
- 公式中的数字、字母统一为 Times New Roman。
- 变量、常量、下标等按数学排版规范处理。

---

## 二、表格模板

### 1. 表格编号规则
- 按章编号，格式为 `表X.X`。
- 表序与表名置于表上方、居中。
- 表名末尾无标点。

### 2. 表格标题模板
```latex
\begin{table}[htbp]
\centering
\caption{系统硬件模块组成与功能}
\begin{tabular}{cccc}
\toprule
模块类别 & 核心器件 & 主要功能 & 备注 \\
\midrule
主控模块 & STM32F103RCT6 & 任务调度、网络通信、联动决策 & 裸机/FreeRTOS可切换 \\
\bottomrule
\end{tabular}
\end{table}
```

### 3. 表格排版要求
- 删除左右边线。
- 表头无斜线。
- 表头与表格不得跨页拆分。
- 表内文字统一五号宋体。
- 起行空一格，转行顶格，句末无标点。
- 表注用小五号宋体置于表下，多条附注用阿拉伯数字编号。

### 4. 单位与空值
- 若全表统一单位，单位放在表头右上角。
- 空缺单元格统一用 `－`。
- 删除所有“同上”类表述。

---

## 三、插图模板

### 1. 插图编号规则
- 按章编号，格式为 `图X.X`。
- 图题置于图下，五号宋体。
- 分图用 `(a)`、`(b)` 标注，置于分图下方。

### 2. 插图模板
```latex
\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{fig3_1.png}
\caption{系统总体框图}
\label{fig:system_block}
\end{figure}
```

### 3. 插图排版要求
- 插图与图题不得跨页拆分。
- 空白不足时，将图移至次页最前面。
- 坐标图必须标注坐标轴说明与坐标单位。
- 显微照片必须标注放大倍数。
- 引用的插图应在图题右上角标注参考文献编号。

### 4. 分图模板
```latex
\begin{figure}[H]
\centering
\subfigure[(a)]{\includegraphics[width=0.45\textwidth]{a.png}}
\hspace{0.02\textwidth}
\subfigure[(b)]{\includegraphics[width=0.45\textwidth]{b.png}}
\caption{对比图}
\label{fig:compare}
\end{figure}
```

---

## 四、目录与引用模板

### 1. 图表引用
- 图引用：`如图3.1所示`
- 表引用：`见表3.1`
- 公式引用：`见式(3.1)`

### 2. 目录同步要求
- 图题、表题、章节标题修改后，目录必须同步更新。
- 目录标题与正文标题文字完全一致。

---

## 五、Word 样式建议

### 公式
- 居中
- 编号右对齐
- 正文引用统一为“见式(1.1)”

### 表格
- 表题上方
- 五号黑体加粗
- 表内容五号宋体
- 居中对齐

### 图片
- 图片居中
- 图题下方居中
- 五号宋体

---

## 六、LaTeX 样式建议

```latex
\usepackage{booktabs}
\usepackage{caption}
\usepackage{float}
\usepackage{array}
\usepackage{amsmath}

\captionsetup[table]{font={small,bf},labelfont={bf},labelsep=space}
\captionsetup[figure]{font=small,labelfont=normalfont,labelsep=space}

\renewcommand{\thefigure}{\thechapter.\arabic{figure}}
\renewcommand{\thetable}{\thechapter.\arabic{table}}
\numberwithin{equation}{chapter}
```

---

## 七、不能直接修正的问题

以下问题需要你手动补图或补源数据：
- 图纸缺失
- 公式源代码未提供
- 图表实际内容缺少坐标轴、单位或放大倍数
- 某些图表并没有对应的参考文献来源编号

---

## 八、建议
- 图、表、公式修改后务必同步更新目录和交叉引用。
- 最终版提交前，建议再统一检查一次字体、页码、行距和分节。
